{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/netcore_websocket_client.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}